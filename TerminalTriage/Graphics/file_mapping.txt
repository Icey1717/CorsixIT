-- Terminal Triage custom graphics mapping
-- Loaded by CorsixTH when use_new_graphics = true in config.txt.
--
-- FORMAT for animations: a sequential array of custom animation filenames (.dat).
-- CorsixTH iterates this list with ipairs and calls loadCustomAnims() on each entry.
--
--   file_mapping = {
--     "custom_anim_01.dat",
--   }
--
-- FORMAT for sprite sheet overrides (via App:readDataFile override in app.lua):
-- sprite_mapping is a key-value table mapping original sprite names to replacement
-- filenames.  Override files (.tab + .dat pairs) must be placed under:
--
--   TerminalTriage/Graphics/Data/{name}.tab   (and .dat)
--   TerminalTriage/Graphics/QData/{name}.tab  (and .dat)
--
-- The App:readDataFile override installed by TerminalTriage/Lua/app.lua checks
-- these directories automatically for every sprite sheet request, so listing names
-- in sprite_mapping is optional documentation — the filesystem check is implicit.
--
--   sprite_mapping = {
--     Panel02V = "Graphics/Data/Panel02V.tab",   -- replaces hospital bottom panel
--     Req05V   = "Graphics/QData/Req05V.tab",    -- replaces hire dialog sprites
--   }
--
-- Leave tables empty until actual replacement files are ready.
file_mapping   = {}
sprite_mapping = {}

