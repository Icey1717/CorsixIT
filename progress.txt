## Terminal Triage — Development Progress Summary

- **Phase 1 (Bootstrap):** Established the mod package layout under `TerminalTriage/`; created Windows/Linux launchers using `--lua-dir` + `--config-file`; built a minimal 4-file Lua root (utility/strict/class stubs + bootstrapper app.lua); fixed `App:getFullPath` to resolve to `TerminalTriage/` by capturing `debug.getinfo` before `dofile`; patched `corsixth.require` to try mod dir first with fallback to base Lua tree; installed busted 2.3.0 test framework; proved campaign/level discovery and graphics override path resolution; created smoke-test checklist and developer workflow docs (43 tests passing).
- **Phase 2 (Text Layer):** Fully populated `english_terminal_triage.lua` with IT-themed overrides for all player-visible strings — staff titles, 26 room names, 36 diseases (Battery Balloon, Tab Tsunami, Ransomwear, Thermal Meltdown, etc.), 70+ adviser messages, fax/VIP/epidemic notifications, 35 transaction labels, graph/casebook/research labels, and all key tooltips; 36 new TDD tests; no hospital terminology leaks (79 tests passing).
- **Phase 3 (Campaign Content):** Expanded campaign from stub to 3-level progression — Level 1 (tutorial, no win conditions), Level 2 (Neighbourhood Repair Shop, 7 diseases), Level 3 (Student Tech Hub, 14 diseases, emergencies, seasonal swings); maps copied from CorsixTH base; 26 new level content tests (105 tests passing).
- **Phase 4.1 (Sprite Override):** First added `App:readDataFile` Lua monkey-patch to intercept sprite requests and check `TerminalTriage/Graphics/Data|QData/` before the TH install; then replaced that entirely with a proper C++ hook — added `overlay_paths_` + `add_overlay_path()`/`get_overlay_paths()` to `iso_filesystem`, Lua bindings for `provider:addSearchPath()`, `FileSystem:addSearchPath()` in directory mode, and auto-registration in `Graphics:_load` when `use_new_graphics=true`; removed the 32-line monkey-patch from app.lua (114 tests passing).
- **Phase 5.1 (Grim Reaper Removal):** Created `humanoid_actions/die.lua` override that always despawns cleanly after the fall animation, suppressing the heaven-ascension and grim-reaper+lava-hole sequences that break the IT fiction; added `App.loadLuaFolder` merge override so base actions are not silently dropped when the mod has its own `humanoid_actions/` directory; preserved `permanent()` keys for save/load compatibility (131 tests passing).
- **Lua Tree Migration:** Replaced the entire bootstrap fallback architecture (corsixth.require patch, getFullPath override, loadLuaFolder merge, base_config.lua stub, strings.lua two-pass override) with a full copy of `CorsixTH/Lua/` into `TerminalTriage/Lua/`; all engine files now resolve directly with zero patching; mod overrides (die.lua, english_terminal_triage.lua) sit in-tree alongside engine files; game launches cleanly from `launch.bat` with no errors.
- **Phase 5.2 (Disease Contagion Alignment):** Reviewed all 34 disease `contagious` flags and aligned them with IT fiction — 15 diseases updated so that only genuine network/malware infections (Ransomwear, Privacy Spill, Phishing Hangover, Latency Gremlins, Software Rot, Registry Rot, Update Loop Fever, Data Corruption) can trigger malware-outbreak epidemic events; 14 hardware/user-error diseases (Battery Balloon, Port Collapse, Overclocking Incident, Folder Catastrophe, etc.) made non-contagious; `transparency` (Privacy Spill) promoted to contagious; 34 new TDD tests covering every disease contagious flag (175 tests passing).
- **Phase 4.2 (Hospital String Leak Patch):** Audited remaining hospital-terminology leaks in `english_terminal_triage.lua` and added all missing IT overrides: `information.level_lost` (fail-screen header plus `percentage_killed`, `patient_happiness`, `balance`, `reputation`, `staff_happiness`, `cheat` keys); `information.custom_game`; `tooltip.casebook.cure_requirement.hire_staff` and `tooltip.casebook.cure_type.unknown`; `tooltip.objects.litter` and `tooltip.objects.rathole`; `room_descriptions.inflation[2]` (Battery Repair Bay description, no Bloaty Head); full `totd_window.tips` replacement with 20 IT-themed tips (no hospital/patient/doctor/GP's office). Also expanded the test sandbox with `information`, `room_descriptions`, `totd_window` auto-tables. 15 new TDD tests added (190 tests passing).
- **CorsixIT-a0p (PA Subtitle Announcements):** Added complete IT-themed `subtitles = { ... }` block to `english_terminal_triage.lua` covering all 8 epidemic/malware-outbreak alerts (epid001–008), all 34 surge-job emergency announcements (emerg001–034) using IT disease names, 13 maintenance announcements using IT room names (Facilities Tech/Battery Repair Bay/etc.), 20 staff-required announcements using IT staff titles (Technician/Bench Technician/etc.), 8 staff-dismissal lines, random PA announcements, VIP lines; also added `misc.epidemics_off/on`, `misc.epidemic_no_*`, `dynamic_info.staff.actions.vaccine`, `dynamic_info.patient.actions.epidemic_vaccinated` and `no_gp_available`. Fixed test sandbox to use plain `subtitles = {}` so nil checks properly detect missing overrides. 88 new TDD tests added (278 tests passing).
- **CorsixIT-ij6 (Gameplay Scenario Script):** Created `TerminalTriage/Lua/tests/simulation_scenario.lua` — a complete headless gameplay scenario that (1) asserts Level 1 loaded with diseases, (2) hires a receptionist and a technician using the same `StaffProfile` + `newEntity` + camera-tile placement pattern as `Hospital:initStaff()`, (3) attempts to place a reception desk at a valid hospital tile (`pcall`-guarded), assigns the receptionist to it, (4) runs 500 warm-up ticks via `app:runSimulatedTicks(500)`, (5) advances to Year 1 Month 3 via `world:advanceToDate(1, 3, 1)`, and (6) asserts the date reached the target. Also added `TerminalTriage/Luatest/spec/simulation_scenario_spec.lua` with 12 new busted unit tests covering placement fallbacks, pcall error handling, date assertion conditions, and world pre-condition validation. Updated `test_runner.bat` usage comment with the scenario example. 12 new tests; 325 total passing.
- **CorsixIT-2ka (Simulation Test Assertions and CI Integration):** Extended `simulation_scenario.lua` with four new post-simulation assertions: (1) hospital balance is non-zero (confirms simulation actually ran), (2) world entity list has no nil slots (walks `world.entities` with ipairs), (3) room list is not corrupted (every `world.rooms` entry must be a table with a non-nil `id`), and (4) customer activity accounting (`num_cured + num_deaths + num_visitors`; warns but does not hard-fail in pure headless mode). Added 11 new busted unit tests in `simulation_scenario_spec.lua` covering balance checks, nil-entity detection, corrupted-room detection, and activity-counter arithmetic. Created `scripts/run_simulation_test.ps1` — a two-tier CI runner that (Tier 1) runs the busted unit test suite and exits non-zero on failure, then (Tier 2) optionally launches CorsixTH with `--headless-test` for full engine integration (skippable via `-SkipIntegration`; notes the SDL_WINDOW_OPENGL blocking issue from CorsixIT-5kv). 11 new tests; 336 total passing.
- **CorsixIT-5kv (Fix Headless SDL):** Patched `CorsixTH/Src/th_gfx_sdl.cpp` in `render_target::render_target()` to detect `SDL_VIDEODRIVER=dummy` or `offscreen` via `SDL_getenv` and skip `SDL_WINDOW_OPENGL` in that case (those drivers have no OpenGL support). `SDL_CreateRenderer` with `-1` still auto-selects the best available backend. Updated `scripts/run_simulation_test.ps1` and `TerminalTriage/test_runner.bat` to now unconditionally set `SDL_VIDEODRIVER=dummy`, removing the old "requires a real display" caveat. Build verified clean; 336 tests still passing.



